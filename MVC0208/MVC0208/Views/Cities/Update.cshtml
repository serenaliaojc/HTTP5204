@model MVC0208.Models.City
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Update</title>
</head>
<body>
    <h2>Update</h2>

    @using (Html.BeginForm("Update", "Cities"))
    {
        <div>
            @Html.HiddenFor(m => m.Id)
            <div>
                @Html.LabelFor(m => m.Name)
            </div>
            <div>
                @Html.EditorFor(m => m.Name)
                @Html.ValidationMessageFor(m => m.Name)
            </div>
        </div>
        <div>
            <div>
                @Html.LabelFor(m => m.Population)
            </div>
            <div>
                @Html.EditorFor(m => m.Population)
                @Html.ValidationMessageFor(m => m.Population)
            </div>
        </div>
        <div>
            <div>
                @Html.LabelFor(m => m.Mayor)
            </div>
            <div>
                @Html.EditorFor(m => m.Mayor)
                @Html.ValidationMessageFor(c => c.Mayor)
            </div>
        </div>
        <div>
            <div>
                @Html.LabelFor(m => m.Country_Code)
            </div>
            <div>
                @Html.EditorFor(m => m.Country_Code)
            </div>
        </div>
        <div>
            <input type="submit" value="Update" />
        </div>
    }
</body>
</html>
